import React from 'react';
import PropTypes from 'prop-typs';

export default function Q3({ onChange, q2State }) {
  return (
    <section>
      <h3>
        What is the <strong>bind</strong> method used for?
      </h3>
      Merging 2 objects

      Merging Arrays

      Lets you bind a function to an object in the global scobe

      Lets you bind Everything in the local scope.

    </section>
  );
}
